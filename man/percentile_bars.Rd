% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile_bars.R
\name{percentile_bars}
\alias{percentile_bars}
\title{Build inline percentile bars as HTML.}
\usage{
percentile_bars(
  values,
  scaled = TRUE,
  labels = TRUE,
  label_cutoff = 0.4,
  decimals = 1,
  height = 16,
  width = 100,
  fill = "purple",
  background = "#e1e1e1",
  font_style = "bold",
  font_size = "10px"
)
}
\arguments{
\item{values}{Numeric vector. Percentiles in 0–100 when \code{scaled = TRUE},
or arbitrary nonnegative numbers when \code{scaled = FALSE} (in which case
they're normalized against \code{max(values, na.rm = TRUE)}).}

\item{scaled}{If \code{TRUE}, treat \code{values} as already on a 0–100 scale.
Otherwise rescale to the column maximum.}

\item{labels}{If \code{TRUE}, overlay a percentage label.}

\item{label_cutoff}{Fraction (0–1) of the bar length below which the
label is rendered \emph{outside} the bar rather than centered inside it.}

\item{decimals}{Decimal places shown in the label.}

\item{height}{Bar height in pixels.}

\item{width}{Track width in pixels.}

\item{fill}{Bar fill color.}

\item{background}{Track background color.}

\item{font_style}{Label \code{font-weight} (\code{"bold"}, \code{"normal"}, \code{"italic"}).}

\item{font_size}{Label \code{font-size}, as a CSS length string.}
}
\value{
A list of \code{shiny::HTML()} values, length \code{length(values)}.
}
\description{
Replaces the gt-coupled \code{my_gt_plt_bar_pct()}: returns a list of
\code{shiny::HTML()} values, one per input element, suitable for storing
directly in a \code{data.table} column that \code{html_table} then passes through
verbatim.
}
\details{
The visual is the same as gt's: a fixed-width grey track containing a
colored bar sized to \code{value}. With \code{labels = TRUE}, a small percentage
label is overlaid — inside the bar if the bar is long enough, otherwise
outside it. Missing values render as an em-dash, matching the original.
}
\keyword{internal}

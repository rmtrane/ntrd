% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_table.R
\name{ht_tab_style}
\alias{ht_tab_style}
\title{Attach styles to specific cell locations.}
\usage{
ht_tab_style(x, style, locations)
}
\arguments{
\item{x}{A \code{html_table}.}

\item{style}{A style spec. See description.}

\item{locations}{One \code{\link[=ht_cells_body]{ht_cells_body()}} / \code{\link[=ht_cells_stub]{ht_cells_stub()}} / etc., or a
list of them.}
}
\description{
Mirrors \code{gt::tab_style()}. \code{style} is a named character vector of CSS
properties (most easily built with \code{\link[=ht_cell_text]{ht_cell_text()}} / \code{\link[=ht_cell_fill]{ht_cell_fill()}}),
a raw CSS string (\code{"padding-left: 0;"}), or a list of any of those.
\code{locations} is a single \verb{ht_cells_*()} object or a list of them.
}
\details{
\code{rows} expressions on body / stub locations are evaluated \emph{here}
against \code{x$data}, not deferred to render time. Free variables in the
expression (e.g., a loop-local \code{desc}) are resolved against the
caller's frame at the moment \code{ht_tab_style()} runs, so loops like
\code{for (desc in ...) out <- ht_tab_style(out, ..., rows = X == desc)}
behave as written.
}
\examples{
\dontrun{
ht_table(dat) |>
  ht_tab_style(
    style = ht_cell_text(weight = "bold", color = "red"),
    locations = ht_cells_body(columns = "value", rows = is.na(value))
  )
}

}

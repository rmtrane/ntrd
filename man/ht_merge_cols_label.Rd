% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_table.R
\name{ht_merge_cols_label}
\alias{ht_merge_cols_label}
\title{Merge column labels into one cell.}
\usage{
ht_merge_cols_label(x, label, columns, align = c("center", "left", "right"))
}
\arguments{
\item{x}{A \code{html_table}.}

\item{label}{String or tag for the merged cell.}

\item{columns}{Character vector of column names to merge (length >= 2).}

\item{align}{One of \code{"left"}, \code{"center"}, \code{"right"}. Default \code{"center"}.}
}
\description{
Replaces the individual \verb{<th>}s for the named columns with a single
\verb{<th colspan=N>} in the column-label row. Use this when several
columns share a unified label (e.g., a \code{"Raw"} header spanning a
value column and a units column).
}
\details{
Differs from \code{\link[=ht_tab_spanner]{ht_tab_spanner()}} in that a spanner adds a \emph{new row}
above the column labels, whereas a merge collapses cells \emph{within}
the column-label row.

The columns to merge must be contiguous in the rendered table after
hidden columns are removed; non-contiguous merges are ignored with a
warning, since they would produce invalid markup.

Any individual labels set via \code{\link[=ht_cols_label]{ht_cols_label()}} for columns inside
the merge group are overridden by the merge's \code{label}. Styles set
via \code{\link[=ht_tab_style]{ht_tab_style()}} / \code{\link[=ht_cells_column_labels]{ht_cells_column_labels()}} on any of the
covered columns combine onto the merged cell.
}
\examples{
\dontrun{
ht_table(dat) |>
  ht_tab_spanner("Scores", c("raw", "raw_suffix", "units", "std")) |>
  ht_merge_cols_label("Raw", columns = c("raw", "raw_suffix"))
}

}

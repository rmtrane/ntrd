% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_table.R
\name{ht_cells}
\alias{ht_cells}
\alias{ht_cells_body}
\alias{ht_cells_stub}
\alias{ht_cells_column_labels}
\alias{ht_cells_stubhead}
\alias{ht_cells_row_groups}
\alias{ht_cells_footnotes}
\title{Locations for \code{\link[=ht_tab_style]{ht_tab_style()}}}
\usage{
ht_cells_body(columns = NULL, rows = NULL)

ht_cells_stub(rows = NULL)

ht_cells_column_labels(columns = NULL)

ht_cells_stubhead()

ht_cells_row_groups(groups = NULL)

ht_cells_footnotes()
}
\arguments{
\item{columns}{Character vector of column names, or \code{NULL} for all.}

\item{rows}{Expression evaluated against the data. May yield a logical
vector, integer indices, or \code{NULL} for all rows.}

\item{groups}{Character vector of group names, or \code{NULL} for all.}
}
\value{
An object inheriting from \code{ht_loc}.
}
\description{
Each returns an \code{ht_loc} object naming a region of the table.
\code{rows} arguments use non-standard evaluation: write expressions
referencing the original data columns (e.g., \code{rows = is.na(value)}).
}
\details{
Free variables in \code{rows} (e.g., a loop-local \code{desc}) are resolved
against the calling frame at the moment \code{\link[=ht_tab_style]{ht_tab_style()}} runs — not
deferred to render time — so writing loops like
\code{for (desc in ...) ht_tab_style(..., rows = Description == desc)}
behaves as written.
}

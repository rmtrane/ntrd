% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_table.R
\name{ht_add_css}
\alias{ht_add_css}
\title{Attach scoped CSS to a single table.}
\usage{
ht_add_css(x, css)
}
\arguments{
\item{x}{A \code{html_table}. Must have been constructed with a non-empty
\code{id}.}

\item{css}{A single character string of CSS rules.}
}
\description{
Every selector in \code{css} is prepended with \verb{#<id>} (where \code{id} is the
value passed to \code{\link[=ht_table]{ht_table()}}), so the styles only affect this table.
\verb{@media}/\verb{@supports}/\verb{@container} blocks are recursed into; other
\code{@}-rules pass through untouched.
}
\details{
To style the \verb{<table>} element itself, use \code{&} (SCSS / native CSS
nesting convention) — it is replaced with \verb{#<id>} directly, without a
descendant combinator. So \verb{& \{ border: 1px solid; \}} becomes
\verb{#<id> \{ border: 1px solid; \}}, whereas plain \verb{tr \{ ... \}} becomes
\verb{#<id> tr \{ ... \}}.
}
\examples{
\dontrun{
ht_table(dat, id = "demographics-table") |>
  ht_hide_header() |>
  ht_add_css("
    & {
      border: 1px solid #333;
    }
    tr:first-child { font-weight: bold; }
    td { padding: 2px 8px; }
  ")
}

}

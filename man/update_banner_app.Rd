% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/updateBannerModule.R
\name{update_banner_app}
\alias{update_banner_app}
\title{Self-contained Shiny app for testing the update banner module}
\usage{
update_banner_app(fake_update = NULL)
}
\arguments{
\item{fake_update}{Optional override for the \code{update_info} reactive.
When \code{NULL} (the default), the real \code{update_info} from
\code{dataSelectServer} is used. When \code{TRUE}, a demo update is shown
regardless of which source is selected. When an \code{update_result}
object, that result is shown directly. Useful for iterating on the
banner visuals without depending on a real version mismatch.}
}
\value{
A \code{shiny::shinyApp} object.
}
\description{
Mounts \code{dataSelectServer} and \code{update_banner_server} together with no
other app machinery, to verify the banner renders correctly from the
\code{update_info} reactive produced by the data-select module.
}
\keyword{internal}

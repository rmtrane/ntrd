% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_result.R
\name{update_result}
\alias{update_result}
\title{Result of an extension update check}
\usage{
update_result(
  available = FALSE,
  current = NA_character_,
  latest = NA_character_,
  news_url = NA_character_
)
}
\arguments{
\item{available}{Logical scalar. Defaults to \code{FALSE}.}

\item{current}{Character scalar. Defaults to \code{NA_character_}.}

\item{latest}{Character scalar. Defaults to \code{NA_character_}.}

\item{news_url}{Character scalar. Defaults to \code{NA_character_}. Use \code{NA}
to indicate that no NEWS URL is available.}
}
\value{
An \code{update_result} S7 object.
}
\description{
An S7 class representing the outcome of an \code{ntrd_update_available()} call.
Extension authors writing custom update checks should end their function
with a call to \code{update_result()} so that the framework receives a
well-typed value. The constructor's defaults make the "nothing to report"
case trivial: \code{update_result()} is a valid no-update-available result.
}
\details{
Properties:
\describe{
\item{\code{available}}{Logical scalar. \code{TRUE} when a newer version of the
extension is known to exist. Defaults to \code{FALSE}, which is the safe
default: a forgotten field will not produce a spurious update
prompt.}
\item{\code{current}}{Character scalar. The installed version of the
extension, or \code{NA_character_} if it could not be determined.}
\item{\code{latest}}{Character scalar. The latest available version, or
\code{NA_character_} if it could not be determined.}
\item{\code{news_url}}{Character scalar. An optional URL pointing to a
changelog or release notes to show alongside the update prompt; use
\code{NA_character_} (the default) when none is available. Consistent with
\code{current} and \code{latest}, \code{NA} represents "absent".}
}

Validation is enforced by the class: invalid construction errors at the
call site. The framework wraps construction in error handling
(see \code{validate_update_check_result()}), so a misbehaving extension cannot
crash the app — it will be downgraded to a safe "no update" result with
a warning.
}
\examples{
# The empty constructor — "no update available", safe default
update_result()

# Couldn't fetch the remote version, but we know what's installed
update_result(current = "0.1.0")

# An update is available
update_result(
  available = TRUE,
  current = "0.1.0",
  latest = "0.2.0",
  news_url = "https://github.com/example/pkg/blob/HEAD/NEWS.md"
)

# No NEWS URL available — use NA, not NULL
update_result(
  available = TRUE,
  current = "0.1.0",
  latest = "0.2.0",
  news_url = NA_character_
)

}

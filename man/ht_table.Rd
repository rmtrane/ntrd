% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_table.R
\name{ht_table}
\alias{ht_table}
\title{Build an HTML table (first-cut: structure only)}
\usage{
ht_table(data, id = NULL, rowname_col = NULL, groupname_col = NULL)
}
\arguments{
\item{data}{A data.frame / data.table.}

\item{id}{Optional string used as the \verb{id=} attribute on the rendered
\verb{<table>}.}

\item{rowname_col}{Optional string naming the column to be rendered as
the row stub (\verb{<th scope="row">}).}

\item{groupname_col}{Optional string naming the column to be rendered
as row group headers.}
}
\value{
A \code{html_table} object.
}
\description{
Constructs a \code{html_table} accumulator. Layered ops (column spanners,
hidden columns, etc.) are added via \verb{ht_*} functions and the final
object is rendered with \code{\link[=ht_render]{ht_render()}} for use inside \code{shiny::renderUI()}.
}

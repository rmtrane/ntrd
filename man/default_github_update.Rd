% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/default_update_functions.R
\name{default_github_update}
\alias{default_github_update}
\alias{default_github_update_available}
\alias{default_github_update_extension}
\title{Default update-check and install functions for GitHub-hosted extensions}
\usage{
default_github_update_available(repo, package = NULL)

default_github_update_extension(repo)
}
\arguments{
\item{repo}{A GitHub repository specification of the form
\code{"user/repo"}. May optionally include a ref (\code{"user/repo@ref"}); the ref
is stripped for purposes of deriving the package name but preserved in
the install call.}

\item{package}{Optional package name. If \code{NULL} (the default), it is
derived from \code{repo} by taking the part after \code{"/"} and stripping any
\verb{@ref} suffix. Provide explicitly when the repository name differs from
the R package name.}
}
\value{
For \code{default_github_update_available()}: a function taking no arguments
that returns an \code{\link{update_result}} S7 object.

For \code{default_github_update_extension()}: a function taking no arguments
that performs the install and returns invisibly.
}
\description{
Factory functions that produce the two functions an extension must export
to opt in to ntrd's in-app update mechanism (\code{ntrd_update_available()} and
\code{ntrd_update_extension()}). Extensions hosted on GitHub can use these
directly; extensions hosted elsewhere (GitLab, CRAN, internal repos)
should provide their own implementations following the same contract.
}
\details{
\code{default_github_update_available()} returns a function that fetches the
\code{DESCRIPTION} file from the GitHub repository's default branch, parses out
the \verb{Version:} field, and compares it against the locally installed
version. The result is wrapped in an \code{\link{update_result}} object.

\code{default_github_update_extension()} returns a function that calls
\code{\link[remotes:install_github]{remotes::install_github()}} with \code{upgrade = "always"} and
\code{dependencies = TRUE}, so dependencies (including \code{ntrd} itself) are also
brought up to date.

Both factories call \code{force()} on their arguments so that captured values
are safe to use even if the factory is ever called in a loop.
}
\examples{
\dontrun{
# In an extension package's R/updates.R:

#' @export
ntrd_update_available <- ntrd::default_github_update_available(
  "rmtrane/ntrdWisconsin"
)

#' @export
ntrd_update_extension <- ntrd::default_github_update_extension(
  "rmtrane/ntrdWisconsin"
)
}

}

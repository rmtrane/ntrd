# Set the stubhead label (top-left cell, only shown when `rowname_col` is set).

Set the stubhead label (top-left cell, only shown when `rowname_col` is
set).

## Usage

``` r
ht_tab_stubhead(x, label)
```

## Arguments

- x:

  A `html_table`.

- label:

  String or tag.

# Add a column spanner across one or more columns.

Add a column spanner across one or more columns.

## Usage

``` r
ht_tab_spanner(x, label, columns)
```

## Arguments

- x:

  A `html_table`.

- label:

  String or tag rendered above the spanned columns.

- columns:

  Character vector of column names to span.

# Shiny app header

Shiny app header

## Usage

``` r
shinyApp_header()
```

## Value

A
[`shiny::tagList`](https://rstudio.github.io/htmltools/reference/tagList.html)
used to add `www/scripts.js` and `www/styles.css` to the head of the
app, and adding the spinner layers.
